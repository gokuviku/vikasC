// Online C compiler to run C program online
#include <stdio.h>
void main() {
    // Write C code here
    float b,h,area;
    // area=0.5*b*h;
    printf("enter base and height");
    scanf("\n %f %f",&b,&h);
    printf("\n area of triangle is : %.2f",b*h*0.5);
    
}